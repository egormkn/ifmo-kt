#include <stdio.h>

int main(int argc, char **argv)
{
	switch(9){
		case 5: printf("5");
		break;
	}
	/*int n = 0, x1 = 1, xy1, x2, y2;
	scanf("%d", &n);
	
	int **A = malloc(n*sizeof(int*) + n*n*sizeof(int));
    char * pc = A;
    pc += n*sizeof(int*);
    for (int i=0; i<n; i++){
        A[i] = pc + i*sizeof(n*sizeof(int));
	}
	
	scanf("%d", &x1);
	while(x1 != 0){
		scanf("%d %d %d", &y1, &x2, &y2);
		
		
		scanf("%d", &x1);
	}
	
	free(A);*/
	printf("- %d", 8);
	return 0;
}

