#include <iostream>
#include <fstream>
#include <set>

using namespace std;
ifstream fin("example.in");
ofstream fout("example.out");
set nums;


int main(){
    string prefix;
    while(cin >> cmd){
        if(cmd == "number"){
            cin >> num1;
            if (num1)
        }
    }
    cout << "Hello world!" << endl;
    return 0;
}
